# Dota 2 AFK Automator

Автоматизация AFK-игры в Dota 2 на режимах "DOTALAND" и "Infinite Mode" с поддержкой Telegram-бота для удаленного управления.

## 📋 Оглавление

- [Описание](#-описание)
- [Особенности](#-особенности)
- [Требования](#-требования)
- [Установка](#-установка)
- [Настройка](#-настройка)
- [Конфигурация питомцев](#-конфигурация-питомцев)
- [Использование Telegram-бота](#-использование-telegram-бота)
- [Горячие клавиши](#-горячие-клавиши)
- [Отладка и решение проблем](#-отладка-и-решение-проблем)

## 🎮 Описание

Данный скрипт автоматизирует процесс AFK-игры в Dota 2 на специальных режимах:
- **Обычный режим**: Поиск лобби "DOTALAND" по паролю, вход и мониторинг до конца игры
- **Режим по приглашению**: Ожидание приглашения в лобби с авто-принятием
- **Infinite Mode**: Автоматический вход/выход из бесконечной арены с отслеживанием статистики

Скрипт автоматически:
- Запускает Dota 2 с нужными параметрами
- Переходит в раздел Arcade и находит лобби по паролю
- Вводит пароль (поддерживает русские символы)
- Нажимает кнопки ACCEPT/OK
- Отслеживает состояние игры (красная рамка аватарки, текст в чате)
- Управляет бесконечным режимом
- Переключает питомцев по триггерам
- Отправляет уведомления в Telegram

## ✨ Особенности

- **Умный ввод пароля**: Автоматическое переключение раскладки для русских/английских символов
- **Поиск по цвету**: Обнаружение кнопок по цвету для бесконечного режима
- **Распознавание текста**: Использование Tesseract OCR для поиска интерфейсных элементов
- **Отслеживание состояния**: Мониторинг смерти хоста по цвету рамки аватарки и тексту в чате
- **Telegram-интеграция**: Полный контроль через Telegram-бота
- **Статистика**: Подробная статистика сессий, смертей хоста, циклов бесконечки
- **Управление паузами**: Горячие клавиши для управления скриптом
- **Настраиваемые питомцы**: Триггеры для автоматического переключения питомцев

## 📋 Требования

### Системные требования
- **Windows 10/11** (тестировалось на Windows)
- **Разрешение экрана**: 2560x1440 (2K) - скрипт оптимизирован под это разрешение
- **Раскладка клавиатуры**: Английская (скрипт автоматически переключает при необходимости)
- **Язык Dota 2**: Английский (обязательно!)

### Оборудование
- Мышь и клавиатура должны быть доступны для эмуляции ввода
- Dota 2 должна быть запущена в оконном или оконном полноэкранном режиме

### Программные требования
- **Python 3.8+**
- **Tesseract OCR** (обязательно для распознавания текста)
- **Dota 2** с установленным режимом DOTALAND
- **Steam** (запущенный в фоне)

## 🚀 Установка

### 1. Клонирование репозитория
```bash
git clone https://github.com/ваш-репозиторий/dota2-afk-automator.git
cd dota2-afk-automator
```

### 2. Установка Tesseract OCR
1. Скачайте Tesseract с официального сайта: https://github.com/UB-Mannheim/tesseract/wiki
2. Установите в `C:\Program Files\Tesseract-OCR\`
3. Проверьте наличие файла `tesseract.exe` по пути `C:\Program Files\Tesseract-OCR\tesseract.exe`

### 3. Установка Python зависимостей
```bash
pip install -r requirements.txt
```

**ИЛИ вручную:**
```bash
pip install pyautogui pillow pytesseract keyboard psutil pyperclip python-telegram-bot
```

### 4. Настройка разрешения экрана
Скрипт оптимизирован для разрешения **2560x1440 (2K)**. Если у вас другое разрешение:

1. Откройте `config.py`
2. Найдите настройки области поиска
3. Измените координаты и размеры областей под ваше разрешение

### 5. Настройка Dota 2
**ВАЖНО:** Все настройки должны быть выполнены точно!

1. **Язык игры**: Установите английский язык в настройках Steam/Dota 2
2. **Раскладка клавиатуры**: В системе должна быть доступна английская раскладка
3. **Настройки позиции #5**: В Dota 2 настройте позицию #5 на клавишу **`** (обратная кавычка/тильда)
4. **Режим отображения**: Оконный или оконный полноэкранный
5. **Проверьте элементы интерфейса**:
   - Кнопка "ARCADE" должна быть видна
   - Кнопка "LIBRARY" должна быть видна
   - Кнопка "LOBBY" должна быть видна
   - Кнопка "FIND" должна быть доступна

## ⚙️ Настройка

### Основной конфигурационный файл `config.py`

Откройте `config.py` и настройте параметры:

```python
# Основные настройки
PASS_LOBBY = "ваш_пароль"  # Пароль лобби DOTALAND
START_FROM = 1  # Точка старта: 1=начало, 2=FIND, 3=AFK мониторинг
WORK_MODE = 1  # Режим работы: 1=обычный, 2=по приглашению

# Настройки бесконечного режима
INFINITE_MODE_ENABLED = True  # Включить бесконечный режим
INFINITE_KEY_PRESS = "`"  # Клавиша для позиции #5
INFINITE_CLICK_X = 1263  # Координата X для клика (под ваше разрешение)
INFINITE_CLICK_Y = 415  # Координата Y для клика (под ваше разрешение)

# Telegram бот
TELEGRAM_BOT_TOKEN = "ваш_токен"  # Токен от @BotFather
TELEGRAM_BOT_ENABLED = True  # Включить бота
TELEGRAM_ADMIN_IDS = [1997218377]  # Ваш Telegram ID

# Логирование
ENABLE_DEBUG_SCREENSHOTS = False  # Включить отладочные скриншоты
```

### Настройка координат для вашего разрешения

Если у вас не 2K разрешение, необходимо настроить координаты:

1. **Для бесконечного режима** (`config.py`):
   - `INFINITE_CLICK_X` - X координата клика
   - `INFINITE_CLICK_Y` - Y координата клика
   - Эти координаты указывают на кнопку входа в бесконечную арену

2. **Для питомцев** (`configs/pets_config.json`):
   - Каждому питомцу настройте координаты кликов
   - Используйте `region_setup.py` для определения координат

3. **Для поиска элементов интерфейса**:
   - Области поиска настроены в `avatar_monitor.py`
   - При необходимости измените `search_region` в методах поиска

### Тестовая проверка настроек

1. Запустите Dota 2 вручную
2. Запустите скрипт с параметром `START_FROM = 3` для тестирования AFK мониторинга
3. Проверьте, что скрипт находит стрелку '>' и рамку аватарки

## 🐾 Конфигурация питомцев

### Создание питомца через Telegram-бота
1. Включите Telegram-бота
2. Отправьте команду `/add_pet pet1 "Питомец 1"`
3. Настройте координаты кликов:
   ```
   /add_click pet1 x y "Описание"
   ```
4. Установите задержку между кликами:
   ```
   /set_delay pet1 2.0
   ```

### Настройка триггеров для автоматического переключения
Триггеры срабатывают при достижении определенного количества циклов бесконечки:

```
/add_trigger pet1 50  # Сработает при 50 циклах
/add_trigger pet1 100 # Сработает при 100 циклах
```

### Ручная настройка в файле
Файл `configs/pets_config.json`:
```json
{
  "pet1": {
    "name": "Питомец 1",
    "click_delay": 2.0,
    "infinite_triggers": [
      {"cycles": 50, "enabled": true},
      {"cycles": 100, "enabled": true}
    ],
    "clicks": [
      {"x": 100, "y": 100, "description": "Клик 1"},
      {"x": 200, "y": 100, "description": "Клик 2"}
    ]
  }
}
```

## 🤖 Использование Telegram-бота

### Создание бота
1. Найдите @BotFather в Telegram
2. Отправьте `/newbot`
3. Укажите имя бота
4. Получите токен
5. Вставьте токен в `config.py` в `TELEGRAM_BOT_TOKEN`

### Основные команды
- `/start` - Начало работы с ботом
- `/status` - Текущий статус программы
- `/pause` - Поставить на паузу
- `/resume` - Продолжить работу
- `/restart` - Перезапустить программу
- `/shutdown` - Завершить программу
- `/stats` - Показать статистику
- `/password новый_пароль` - Изменить пароль лобби
- `/pet_list` - Список питомцев
- `/add_pet pet1 "Имя питомца"` - Добавить питомца
- `/switch_to pet1` - Переключиться на питомца

### Управление через бота
Бот позволяет:
- Удаленно запускать/останавливать скрипт
- Изменять пароль лобби без перезапуска
- Мониторить состояние игры
- Получать уведомления о событиях
- Управлять питомцами

## 🎮 Горячие клавиши

Скрипт поддерживает горячие клавиши для локального управления:

- **F11** - Пауза/продолжение (может не работать в Dota 2)
- **Ctrl+Shift+P** - Пауза/продолжение (работает всегда)
- **F10** - Завершение программы

## 🔧 Отладка и решение проблем

### Включение отладочных скриншотов
В `config.py` установите:
```python
ENABLE_DEBUG_SCREENSHOTS = True
```
Скриншоты будут сохраняться в папке `debug/`

### Распространенные проблемы

1. **"Tesseract not found"**
   - Убедитесь что Tesseract установлен в `C:\Program Files\Tesseract-OCR\`
   - Проверьте наличие `tesseract.exe`

2. **Скрипт не находит элементы интерфейса**
   - Проверьте язык Dota 2 (должен быть английский)
   - Проверьте разрешение экрана
   - Включите отладочные скриншоты для анализа

3. **Проблемы с вводом пароля**
   - Убедитесь что поле ввода пароля активно
   - Проверьте наличие русских символов в пароле
   - Скрипт автоматически переключает раскладку

4. **Бесконечный режим не работает**
   - Проверьте настройку позиции #5 на клавишу ` в Dota 2
   - Настройте координаты `INFINITE_CLICK_X` и `INFINITE_CLICK_Y`
   - Проверьте что игра на бесконечной арене

5. **Telegram бот не отвечает**
   - Проверьте токен в `config.py`
   - Убедитесь что бот запущен (`/start` в Telegram)
   - Проверьте что ваш ID в `TELEGRAM_ADMIN_IDS`

### Логирование
- Основные логи: `log/bot_log.txt`
- Статистика: `log/statistics.json`
- Статистика бесконечки: `log/infinite_stats.json`

### Тестирование отдельных модулей
```python
# Тестирование поиска стрелки
python -c "from avatar_monitor import AvatarMonitor; am = AvatarMonitor(None); am.debug_arrow_search_enhanced()"

# Тестирование поиска рамки
python -c "from avatar_monitor import AvatarMonitor; am = AvatarMonitor(None); am.find_avatar_frame_with_debug()"
```

## 📁 Структура проекта

```
dota2-afk-automator/
├── config.py                    # Основной конфигурационный файл
├── main.py                     # Главный запускающий файл
├── requirements.txt            # Зависимости Python
├── install_req.bat            # Скрипт установки зависимостей
├── README.md                  # Этот файл
│
├── configs/
│   ├── pets_config.json       # Конфигурация питомцев
│   └── infinite.json         # Настройки бесконечного режима
│
├── log/                       # Директория для логов
│   ├── bot_log.txt           # Лог программы
│   ├── statistics.json       # Статистика
│   └── infinite_stats.json   # Статистика бесконечки
│
├── debug/                    # Отладочные скриншоты (создается автоматически)
└── screenshots/              # Скриншоты питомцев (создается автоматически)
│
├── Основные модули:
├── AFK_lobby.py             # Мониторинг AFK лобби
├── avatar_monitor.py        # Поиск элементов интерфейса
├── infinite_mode.py         # Бесконечный режим
├── lobby_navigator.py       # Навигация по лобби
├── pet_manager.py          # Управление питомцами
├── telegram_bot.py         # Telegram бот
├── pause_handler.py        # Обработка пауз
├── statistics.py           # Статистика
├── startGame.py           # Запуск Dota 2
├── text_detector.py       # Распознавание текста
├── config_loader.py       # Загрузчик конфигурации
├── dynamic_config.py      # Динамическая конфигурация
└── region_setup.py        # Настройка регионов
```

## ⚠️ Важные примечания

1. **Используйте на свой страх и риск** - автоматизация может нарушать правила игр
2. **Не оставляйте без присмотра** - возможны непредвиденные ситуации
3. **Регулярно обновляйте** - при обновлениях Dota 2 могут потребоваться изменения
4. **Тестируйте перед использованием** - убедитесь что все работает корректно
5. **Резервное копирование** - сохраняйте свои настройки и конфигурации

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для фичи (`git checkout -b feature/AmazingFeature`)
3. Зафиксируйте изменения (`git commit -m 'Add some AmazingFeature'`)
4. Запушьте ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📄 Лицензия

Распространяется под лицензией MIT. Смотрите файл `LICENSE` для деталей.

## ✉️ Контакты

По вопросам и предложениям создавайте Issues в репозитории.

---

**Примечание**: Этот скрипт создан в образовательных целях. Автор не несет ответственности за возможные блокировки аккаунтов или другие последствия использования автоматизации. Используйте с умом и в соответствии с правилами платформы.
