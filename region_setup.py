import pyautogui
import time

def setup_regions():
    """Вспомогательный скрипт для настройки регионов поиска"""
    print("🎯 Настройка регионов поиска")
    print("Запустите Dota 2 и перейдите в главное меню")
    input("Нажмите Enter когда будете готовы...")
    
    screen_width, screen_height = pyautogui.size()
    print(f"Разрешение экрана: {screen_width}x{screen_height}")
    
    regions = {}
    
    print("\n1. Область верхнего меню:")
    input("Наведите курсор на верхнее меню и нажмите Enter...")
    x1, y1 = pyautogui.position()
    
    print("2. Область центральных кнопок:")
    input("Наведите курсор на центральные кнопки и нажмите Enter...")
    x2, y2 = pyautogui.position()
    
    # Примерные регионы (настройте под ваш экран)
    regions = {
        'top_menu': (x1-200, y1-25, 400, 50),
        'center_buttons': (x2-250, y2-150, 500, 300),
        'left_panel': (100, 200, 300, 600),
        'right_panel': (screen_width-400, 200, 300, 600)
    }
    
    print("\n🎯 Настроенные регионы:")
    for name, coords in regions.items():
        print(f"{name}: {coords}")
    
    return regions

if __name__ == "__main__":
    setup_regions()